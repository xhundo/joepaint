"use client";

const Hamburger = () => {
  return (
    <button
      onClick={async () => {}}
      className="h-icon mr-[30px] self-center lg:hidden"
    ></button>
  );
};

export default Hamburger;
